fragment userFragment on User {
	firstName
	lastName
	email
	id
	profileImage
}

query findAllUsers {
	findAllUsers {
		paging {
			...pagingFragment
		}
		results {
			...userFragment
		}
	}
}

query currentUser {
	currentUser {
		...userFragment
	}
}
