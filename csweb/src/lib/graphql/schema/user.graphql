query findAllUsers {
	findAllUsers {
		paging {
			...pagingFragment
		}
		results {
			firstName
			lastName
			email
			id
		}
	}
}

query currentUser {
	currentUser {
		firstName
		lastName
		email
	}
}
